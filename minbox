#!/bin/sh
# list some inbox 
inbox=${INBOX-$HOME/mail/INBOX}
[ -n "$1" ] && {
	if [ -d "$1" ]; then
		inbox="$1"
	else
		inbox="${HOME}/mail/$1"
	fi
	shift
}
mlist_args="$*"
mlist ${mlist_args} ${inbox} | mthread | mseq -S | mscan
